apiVersion: v1
kind: Service
metadata:
  name: {{ include "svc-openfinance.fullname" . }}
  labels:
    {{- include "svc-openfinance.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "svc-openfinance.selectorLabels" . | nindent 4 }}
